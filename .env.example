# Application Insights Configuration
# Get this from Azure Portal > Application Insights > Overview > Connection String
# Or use Azure CLI: az monitor app-insights component show --app <app-name> --resource-group <rg-name> --query connectionString
APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=00000000-0000-0000-0000-000000000000;IngestionEndpoint=https://your-region.applicationinsights.azure.com/

# Optional: Set log level
# LOG_LEVEL=INFO
